
export class Forums{
    forumTitle;
    forumPostDate;
    forumPost;
    forumUser;
    forumImage;

    constructor(forumTitle, forumPostDate, forumPost, forumUser, forumImage){
        this.forumTitle = forumTitle;
        this.forumPostDate = forumPostDate;
        this.forumPost = forumPost;
        this.forumUser = forumUser;
        this.forumImage = forumImage;
    }
};